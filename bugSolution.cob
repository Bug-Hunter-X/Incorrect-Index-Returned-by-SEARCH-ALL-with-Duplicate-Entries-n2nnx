01  WS-AREA. 
05  WS-COUNT PIC 9(5) VALUE 0. 
05  WS-TABLE OCCURS 100 TIMES INDEXED BY WS-INDEX. 
10  WS-ENTRY PIC X(80). 
05  WS-INDEX-TABLE OCCURS 100 TIMES. 
    10 WS-INDEX-ENTRY PIC 9(5). 

* Some COBOL code that processes data into WS-TABLE

* We now maintain a separate index table
MOVE 0 TO WS-COUNT
PERFORM VARYING WS-INDEX FROM 1 BY 1 UNTIL WS-INDEX > 100
   IF WS-ENTRY(WS-INDEX) = "SPECIFIC-ENTRY" 
       ADD 1 TO WS-COUNT
       MOVE WS-INDEX TO WS-INDEX-TABLE(WS-COUNT)
   END-IF
END-PERFORM

IF WS-COUNT > 0
  DISPLAY "Entry found at indices:"
  PERFORM VARYING WS-INDEX FROM 1 BY 1 UNTIL WS-INDEX > WS-COUNT
     DISPLAY WS-INDEX-TABLE(WS-INDEX)
  END-PERFORM
ELSE
  DISPLAY "Entry not found"
END-IF